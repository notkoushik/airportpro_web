// This code is CORRECT. Use this version.
plugins {
    id 'com.android.application' version '8.2.1' apply false // Using the version from your variables.gradle
    id 'org.jetbrains.kotlin.android' version '1.9.22' apply false // Using the version from your variables.gradle
}

// ✅ Apply your existing variables
apply from: "variables.gradle"

// ✅ Keep your existing clean task
tasks.register('clean', Delete) {
    delete rootProject.buildDir
}