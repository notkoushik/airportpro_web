# Point to JDK 17 (CRITICAL - MUST MATCH SYSTEM JDK)
org.gradle.java.home=C:/Program Files/Eclipse Adoptium/jdk-17.0.16.8-hotspot

# Gradle settings  
org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8 -XX:MaxMetaspaceSize=512m
org.gradle.parallel=true
org.gradle.caching=true

# Android settings
android.useAndroidX=true
android.enableJetifier=true
android.suppressUnsupportedOptionWarnings=true

# Force Java 17 across all modules (CRITICAL FIX)
kotlin.code.style=official
kotlin.jvm.target.validation.mode=WARNING

# Repository management
android.dependencyResolutionManagement.repositories.mode=PREFER_SETTINGS
