# ✅ CRITICAL: Single garbage collector only
org.gradle.jvmargs=-Xmx8g -Dfile.encoding=UTF-8 -XX:MaxMetaspaceSize=2g -XX:+UseG1GC
# ✅ REMOVED: -XX:+UseZGC (conflicting with G1GC)

# ✅ DISABLE parallel to prevent conflicts  
org.gradle.parallel=false
org.gradle.configureondemand=false

# ✅ DISABLE all caching to prevent issues
org.gradle.caching=false
org.gradle.configuration-cache=false
org.gradle.unsafe.configuration-cache=false
org.gradle.build-cache=false

# AndroidX
android.useAndroidX=true
android.enableJetifier=false

# ✅ CRITICAL: Force Java 17 globally
org.gradle.java.home=C:\\Program Files\\Eclipse Adoptium\\jdk-17.0.16.8-hotspot

# Build optimizations
android.suppressUnsupportedOptionWarnings=true
android.defaults.buildfeatures.buildconfig=false
android.defaults.buildfeatures.aidl=false
android.defaults.buildfeatures.renderscript=false
android.defaults.buildfeatures.resvalues=false
android.defaults.buildfeatures.shaders=false
android.useFullClasspathForDexingTransform=false
